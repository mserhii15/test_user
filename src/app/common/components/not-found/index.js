import angular from 'angular';
import notFoundComponent from './not-found.component';
import './not-found.scss';

export default angular.module('notFound', []).component('notFound', notFoundComponent);
