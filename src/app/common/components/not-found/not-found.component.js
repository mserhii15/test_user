import template from './not-found.component.html';
import controller from './not-found.controller';

const notFoundComponent = {
	template,
	controller,
	controllerAs: '$ctrl',
};

export default notFoundComponent;
