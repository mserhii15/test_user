import angular from 'angular';
import forbiddenComponent from './forbidden.component';
import './forbidden.scss';

export default angular.module('forbidden', []).component('forbidden', forbiddenComponent);
